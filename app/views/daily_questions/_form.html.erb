<%= form_for(daily_question) do |f| %>
  <% if daily_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(daily_question.errors.count, "error") %> prohibited this daily_question from being saved:</h2>

      <ul>
      <% daily_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %>
    <%= f.number_field :question_id %>
  </div>

  <div class="field">
    <%= f.label :answer_counter %>
    <%= f.number_field :answer_counter %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
